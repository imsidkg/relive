{
  "buildCommand": "cd ../backend && bun add @trpc/server zod @prisma/client inngest && bunx prisma generate && cd ../frontend && bun run build",
  "devCommand": "bun run dev",
  "installCommand": "bun install",
  "framework": "vite",
  "outputDirectory": "dist"
}
